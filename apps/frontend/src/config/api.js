// Global API URL Configuration
// When deployed to production (e.g., Vercel), this should be set to your Render backend URL.
// For example: export const API_URL = import.meta.env.VITE_API_URL || 'https://karshimart-backend.onrender.com';
// For local development, it falls back to http://localhost:5000.

export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';
